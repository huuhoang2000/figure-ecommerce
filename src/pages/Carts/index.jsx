/* eslint-disable no-unused-vars */
import * as React from 'react';
import { Outlet } from 'react-router-dom';

const Cart = () => {
  return (
    <>
      <Outlet />
    </>
  );
};

export default Cart;
